

# Удаление кнопки «Назначить рук.» из списка коллектива

## Изменение

В `src/pages/UnitDetail.tsx` убрать кнопку «Назначить рук.» из обоих мест:

1. **Десктопная таблица** (строки ~663–675): удалить `<Button>` с текстом "Назначить рук." и связанную логику `setMemberLead` / `setLeadContactId`
2. **Мобильные карточки** (строки ~725–737): аналогично удалить `<Button>` "Назначить рук."

Кнопка удаления (корзина) остаётся как есть. Руководитель по-прежнему отображается с бейджем и назначается через поле в основной информации подразделения.

## Объём
~20 строк удалений в одном файле.

