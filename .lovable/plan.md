

# Увеличить логотип в сайдбаре и на странице входа

## Изменения

### 1. `src/components/AppSidebar.tsx`
- Увеличить логотип с `max-w-[100px]` до `max-w-[160px]` в развёрнутом виде
- В collapsed — с `h-8 w-8` до `h-10 w-10`

### 2. `src/pages/Auth.tsx`
- Увеличить логотип с `h-16` до `h-28` на странице входа

