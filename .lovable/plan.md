

# Переименовать «Заметки» → «Аннотация» и убрать поле «Описание»

## Изменения

### `src/pages/UnitContactDetail.tsx`

1. **Строка 677** — удалить поле «Описание» (Textarea для `description`)
2. **Строка 678** — переименовать label «Заметки» → «Аннотация»
3. **Строка 563** — убрать отображение `p.description` для не-публикаций (или оставить только для обратной совместимости — зависит от того, нужно ли описание для других типов портфолио: проектов, РИД и пр.)

### Уточнение
Поле «Описание» (`description`) убирается **только для публикаций** или для **всех типов** портфолио (проекты, РИД, патенты тоже)? Предполагаю — для всех, раз сказано «убери». Если нужно оставить для не-публикаций, скажите.

### Затронутые файлы
- `src/pages/UnitContactDetail.tsx` — 3 правки (удалить поле описание из формы, переименовать label, убрать отображение)

