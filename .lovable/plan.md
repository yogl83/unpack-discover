

# Вернуть заголовок секции «ПАРТНЕРЫ»

Одна правка в `src/components/AppSidebar.tsx`, строка 78:

| Сейчас | Станет |
|--------|--------|
| `ОРГАНИЗАЦИИ` | `ПАРТНЕРЫ` |

Пункт меню внутри секции («Организации») остаётся без изменений.

