

# Убрать надпись «Партнёрства» из сайдбара

Удалить блок с текстом «Партнёрства» под логотипом в `src/components/AppSidebar.tsx` (строки с `{!collapsed && (<span ...>Партнёрства</span>)}`). Оставить только логотип.

